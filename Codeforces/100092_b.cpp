#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <string>
using namespace std;

int main() {
    freopen("product.in", "r", stdin);
    freopen("product.out", "w", stdout);

    long long a, b;
    cin >> a >> b;

    cout << a * b;
}