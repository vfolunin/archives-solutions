#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <string>
using namespace std;

int main() {
    freopen("sapsan.in", "r", stdin);
    freopen("sapsan.out", "w", stdout);

    int n;
    cin >> n;

    cout << n / 2 / 3 * 4;
}